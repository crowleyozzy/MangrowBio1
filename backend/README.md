`backend` logical layer lives in:
- `src/app/api` for REST routes
- `src/backend/controllers` for route business logic
- `src/backend/middleware` for auth/rate-limit controls
- `src/backend/utils` for validation, sanitization, logging, errors, and email utilities
